---
title: "Callbacks"
metaTitle: "Callbacks | NeuralPy Deep Learning Library"
metaDescription: "Callbacks are classes that perform tasks when we train a model at the end of every epoch. NeuralPy currently supports only one type of Callback, and that is TrainLogger."
---

# Callbacks

```python
neuralpy.callbacks
```

Callbacks are classes that perform tasks when we train a model at the end of every epoch. NeuralPy currently supports only one type of Callback, and that is TrainLogger.

- Callbacks
  - [TrainLogger](/docs/callbacks/train-logger)
